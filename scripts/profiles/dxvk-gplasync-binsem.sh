# Packing DIR
WCP_DIR_64="system32"
WCP_DIR_32="syswow64"

# JSON Target
WCP_MOUNT_64="\${system32}"
WCP_MOUNT_32="\${syswow64}"

WCP_TYPE="DXVK"
WCP_VERSION_CODE=0
WCP_VERSION_SUFFIX="-gplasync-binsem"

WCP_DESC="Built on the Winlator WCP Hub (Upstream: Philip Rebohle, Patch: Ph42oN + binary semaphore fallback)"
